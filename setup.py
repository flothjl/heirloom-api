from setuptools import setup, find_packages

setup(
    name='heirloom_api',
    author='J. Floth',
    version='0.1',
    url='',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[
    ],
)
